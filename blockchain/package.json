{
  "name": "hardhat-project",
  "author": "Guilherme Soares",
  "description": "Full job search tool in blockchain",
  "version": "0.0.1",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat ignition deploy ignition/modules/SmartJob.ts --network Rootstock-Testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.6"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
